@page "{arglot}"
@model NexenHub.Pages.LotModel
@{
}

<div class="container text-light text-center">
    <div class="row g-3">
        <div class="col">
            <h3>@Model.argLOT</h3>
        </div>
    </div>
    <div class="row g-3">
        <div class="col-xl-6">
            @await Component.InvokeAsync("LotItemInfo", Model.lotitem)
        </div>
        <div class="col-xl-6">
            @await Component.InvokeAsync("LotItemHis", Model.lotitem)
        </div>
    </div>
</div>