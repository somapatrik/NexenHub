@page
@model NexenHub.Pages.MachineListModel
@{
}

<div class="alert alert-warning alert-dismissible fade show" role="alert">
    Not called by a procedure
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>

<div>
    <table class="table table-sm table-striped table-hover table-dark">

        <thead>
            <tr>
                @{ foreach (var columnname in Model.DbList.Columns)
                    {
                        <th scope="col">@columnname.ToString()</th>
                    }
                }
            </tr>
        </thead>
        <tbody>

            @{ foreach (System.Data.DataRow row in Model.DbList.Rows)
                {
                    
                    <tr>
                        <th scope="row">
                            <a class="btn-link" href="/MachineProfile/@row[0].ToString()">@row[0].ToString()</a>
                        </th>
                        <td>@row[1].ToString()</td>
                        <td>@row[2].ToString()</td>
                        <td>@row[3].ToString()</td>
                    </tr>
                }
            }

            @*<tr>
            <th scope="row">1</th>
            <td>Mark</td>
            <td>Otto</td>
            <td>mdo</td>
        </tr>
        <tr>
            <th scope="row">2</th>
            <td>Jacob</td>
            <td>Thornton</td>
            <td>fat</td>
        </tr>
        <tr>
            <th scope="row">3</th>
            <td colspan="2">Larry the Bird</td>
            <td>twitter</td>
        </tr>*@
        </tbody>
    </table>
</div>